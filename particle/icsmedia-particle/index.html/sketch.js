

var canvasWidth = 720;
var canvasHeight = 480;
var particleList = [];
var particleSize = 10;
var vx = 0;
var vy = 0;


function setup() {
  createCanvas(canvasWidth, canvasHeight);
    
  noStroke();
  fill(0);
  ellipse(10, 10, particleSize, particleSize);
}

function draw() {
}